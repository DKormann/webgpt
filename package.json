{
  "name": "webgpt",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev-server.ts",
    "build": "bun run scripts/build.ts",
    "preview": "bun run scripts/preview.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@webgpu/types": "^0.1.69"
  }
}

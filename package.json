{
  "name": "webgpt",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "bun run src/dev-server.ts",
    "build": "bun run src/build.ts",
    "preview": "bun run src/preview.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
